# Scripts

建议从仓库根目录执行（`pwd` 在仓库根目录）。

## 目录说明

- `scripts/pipeline/`：一键串联脚本（bash）
- `scripts/steps/`：通用流水线步骤脚本（Python）
- `scripts/transe/`：TransE/KG 相关脚本（Python）
- `scripts/infonce/`：InfoNCE 旧路线脚本（Python）
- `scripts/analysis/`：sweep 日志再解析等（Python）

